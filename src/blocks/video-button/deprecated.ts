/**
 * Deprecated versions of this block's save() output.
 *
 * Intentionally empty on first release — see the identical note in
 * src/blocks/video/deprecated.ts for the rule. Short version: never edit
 * save.tsx without first pushing the current save(), plus the attributes and
 * supports in effect alongside it, into this array (newest first).
 */

const deprecated: any[] = [];

export default deprecated;
